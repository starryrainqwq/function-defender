# 函数防线 (Function Defender) 战斗优化方案

## 概述

本文档针对《函数防线》当前键盘输入过于硬核、战斗爽感不足、操作不够便利的问题，提出系统性的优化方案。核心思路：**降低上手门槛、提升战斗反馈、增强操作流畅度**。

---

## 一、滑块化参数输入（核心优化）

### 1.1 问题分析

当前参数输入方式为纯数字键盘输入（`<input type="number">`），存在以下痛点：
- 快节奏游戏中，键盘输入数字效率低、易出错
- 需要低头看键盘找数字，打断战斗沉浸感
- 参数范围没有直观感受，玩家不知道"调多大才合理"
- 每次发射后参数被清空，需要重新逐位输入

### 1.2 优化方案：双模输入（滑块 + 数字框）

为每个参数同时提供 **滑块（range slider）** 和 **数字输入框**，两者双向同步。

#### 设计细节

```
┌─────────────────────────────────────────┐
│  k  [A]                                 │
│  ◄──●──────────────►  [  1.0  ]        │
│  -5                  5     ▲ 数字框     │
│        ▲ 滑块（绿色发光轨道）              │
│    范围根据参数含义智能调整               │
└─────────────────────────────────────────┘
```

#### 各参数滑块范围建议

| 函数类型    | 参数 | 默认值 | 滑块范围 | 步长  | 说明                         |
|------------|------|--------|---------|-------|------------------------------|
| linear     | k    | 1      | -5~5    | 0.1   | 斜率                         |
| linear     | b    | 0      | -5~5    | 0.1   | 截距                         |
| quadratic  | a    | 1      | -3~3    | 0.1   | 二次系数                     |
| quadratic  | b    | 0      | -5~5    | 0.1   | 一次系数                     |
| quadratic  | c    | 0      | -5~5    | 0.1   | 常数项                       |
| rational   | k    | 1      | -5~5    | 0.1   | 分子系数                     |
| rational   | h    | 0      | -5~5    | 0.1   | 渐近线偏移                   |
| rational   | m    | 0      | -5~5    | 0.1   | 垂直平移                     |
| power      | a    | 1      | -5~5    | 0.1   | 系数                         |
| power      | n    | 2      | 0.5~5   | 0.1   | 指数（正数避免复杂根号）       |
| trig       | A    | 1      | 0.1~5   | 0.1   | 振幅                         |
| trig       | w    | 1      | 0.5~5   | 0.1   | 角频率                       |
| trig       | phi  | 0      | -3.14~3.14 | 0.1 | 相位（-π~π）                 |
| constant_x | k    | 0      | -5~5    | 0.1   | x轴位置                      |
| constant_y | k    | 0      | -5~5    | 0.1   | y轴位置                      |

#### 互动行为

1. **拖动滑块** → 数字框实时更新 + 曲线实时重绘
2. **输入数字框** → 滑块实时跳转到对应位置
3. **鼠标滚轮悬停滑块** → 微调 ±0.1
4. **双击滑块轨道** → 重置为默认值
5. **滑块轨道上显示刻度标记** → 直观感受参数在坐标系中的位置

#### 视觉设计

- 滑块轨道：深色背景 `#001100`，激活色 `#00FF41`，发光阴影 `box-shadow: 0 0 8px #00FF41`
- 滑块把手：圆形，白色边框 + `#00FF41` 填充，hover 放大 120%
- 轨道上以细微刻度线标注整数值
- 数字框风格与现有统一：`bg-black border-[#00FF41]/30 text-[#00FF41] font-mono`

---

## 二、增强战斗爽感

### 2.1 发射波形视觉强化

#### a) 曲线发光脉冲
- **当前**：曲线为静态绿色线条，发射时无变化
- **优化**：发射瞬间曲线触发从发射点向外扩散的 **绿色光环脉冲**（沿曲线方向扫过），持续约 300ms
- 实现方式：在 canvas 上对曲线做 `shadowBlur` 临时增强 + 渐隐

#### b) 发射按钮爆炸动画
- 点击"发射波形"按钮时，按钮发出 **金色波纹扩散**（使用 CSS `@keyframes` radial burst）
- 按钮短暂放大 105% 后弹回

#### c) 屏幕边缘闪光
- 发射时在 canvas 四边出现短暂的 **绿色扫描线** 效果（retro sci-fi 风格）
- `box-shadow` inset 绿色脉冲，持续 200ms

### 2.2 命中反馈强化

#### a) 击杀粒子升级
- **当前**：20个小粒子散开
- **优化后**：
  - 粒子数量提升到 **30~50 个**（随连击数增加而增加）
  - 粒子颜色多样化：除了绿色和白色，加入 **金色**（连击≥3）和 **彩虹色**（连击≥5）
  - 粒子拖尾效果：粒子后方有 2~3px 的渐变拖尾
  - 粒子大小随机 3~8px

#### b) 击中闪光环
- 在命中的怪物位置产生一个 **向外扩散的圆环冲击波**（白色→透明，持续 400ms）
- 连击数越高，冲击波半径越大

#### c) 数字弹出效果
- 命中时在怪物位置弹出 **"+分数"的浮动文字**，向上飘动并渐隐（类似 RPG 伤害数字）
- 连击时文字更大、颜色从绿→金→红渐变

### 2.3 连击系统升级

#### a) 连击计数器动画
- 侧边栏连击显示区增加 **数字跳动动画**（每个数字位独立滚动）
- 连击≥3 时，连击数字外围增加 **旋转的金色光环**

#### b) 连击音效分层
- 连击 1: 基础击中音效
- 连击 3: 加入低频共振音
- 连击 5: 加入高音阶"叮"声
- 连击 10+: 持续的背景嗡鸣（drone）

#### c) Combo 画面特效
- 连击≥5: canvas 边缘出现 **金色边框发光**
- 连击≥10: 背景网格线短暂变成 **金色** 再恢复

### 2.4 屏幕震动增强

- **当前**：简单的 CSS shake 动画
- **优化**：震动强度分级
  - 怪物逃脱: 15px 偏移, 400ms（当前保持一致）
  - 击杀连击: 根据连击数动态计算 `min(hitCount * 3, 20)px`
  - 清屏炸弹: 30px 偏移 + 白色闪屏 200ms
  - 装甲归零: 红色闪屏 + 持续性低频震动

### 2.5 背景氛围增强

- 背景粒子增加更多运动形态（不只是随机飘动）
  - 击杀时周边背景粒子短暂被 **推向四周**（冲击波效果）
  - 冷却期间背景粒子速度减慢
  - 障碍模式时危险区域附近的背景粒子呈红色

---

## 三、操作便利度提升

### 3.1 快速预设系统

在参数面板下方增加一排 **预设按钮**：

```
┌─────────────────────────────────────────┐
│  预设:  [k=±1] [k=±2] [k=±3] [零位]    │
│         [上次] [随机]                    │
└─────────────────────────────────────────┘
```

#### 预设说明

| 按钮       | 快捷键 | 行为                              |
|-----------|--------|----------------------------------|
| k=±1      | —      | 切换 k 在 1 和 -1 之间            |
| k=±2      | —      | 切换 k 在 2 和 -2 之间            |
| k=±3      | —      | 切换 k 在 3 和 -3 之间            |
| 零位       | Z      | 所有参数归零                      |
| 上次       | X      | 恢复上一次发射使用的参数（不清空）    |
| 随机       | C      | 在合理范围内随机生成参数           |

### 3.2 参数记忆与不清空

- **当前行为**：发射后所有参数清空为空字符串
- **优化行为**：
  - 发射后参数 **不清空**，保留上次的值（便于微调后再次发射）
  - 新增 `X` 键：一键恢复上一个函数类型的上次参数
  - 切换函数类型时才重置为新类型的默认值

### 3.3 预设函数曲线

在函数选择区增加一个 **"常用曲线"快捷面板**：

```
┌─────────────────────────────────────────┐
│  快捷曲线:                               │
│  [y=x] [y=-x] [y=x²] [y=1/x] [y=sinx]  │
│  [y=2x] [y=0.5x²] [y=2/x] [y=tanx]    │
└─────────────────────────────────────────┘
```

点击后自动设置对应函数类型和参数，然后自动发射（或仅设置不发射，可配置）。

### 3.4 键盘快捷键扩展

- `1~8` 数字键：直接选择 8 个快捷曲线（与上面预设对应）
- `Tab`：在参数输入框之间快速跳转
- `Ctrl+Z`：撤销参数修改
- `鼠标滚轮`在 canvas 上：缩放坐标系视图（可选功能）

---

## 四、可视化与直观操作增强

### 4.1 参数拖拽点

在 canvas 的曲线上增加 **可拖拽的控制点**：

- 一次函数: 显示截距点（与 y 轴交点），可上下拖拽调整 b
- 二次函数: 显示顶点，可拖拽调整 a 和 b, c
- 三角函数: 显示振幅峰值点，可拖拽调整 A 和 w
- 常数函数: 直接拖拽整条线

#### 实现细节

- 控制点为 10px 白色圆点 + 绿色光环
- 拖拽时实时更新侧边栏参数
- 拖拽时画布显示辅助虚线
- 移动端触摸拖拽适配

### 4.2 怪物预警线提前显示

- 怪物出现后，在其位置用 **淡红色虚线** 标记其坐标范围边界
- 帮助玩家判断函数曲线应当穿过哪些区域
- 怪物即将到达时（< 30% 剩余时间），红色虚线闪烁加速

### 4.3 命中判定可视化

- 当前命中判定对玩家不可见
- 优化：发射后在 canvas 上短暂显示 **淡绿色的命中区域扫描线**（沿曲线扫过），持续 500ms
- 这样玩家可以直观看到"曲线覆盖了哪些区域"

---

## 五、移动端/触屏适配

### 5.1 触摸优化

- 参数滑块宽度放大到至少 44px（符合触屏最小触摸目标）
- 函数选择按钮增大
- 发射按钮增大到全宽、高度至少 56px
- Canvas 支持双指缩放

### 5.2 手势操作

- **单指滑动** 在参数区域：调整当前聚焦参数
- **双指缩放** 在 canvas：调整坐标轴缩放
- **长按发射按钮**：连续发射（自动重复，间隔等于冷却时间）

---

## 六、冷却系统可视化增强

### 6.1 当前问题

冷却时只显示文字 "RECHARGING 1.5s"，缺少视觉张力。

### 6.2 优化方案

#### a) 发射按钮冷却进度条
- 按钮内部底部出现 **金色进度条** 从左到右填充
- 进度条带有粒子火花效果（尾端有小粒子溅射）

#### b) 全局冷却指示器
- 顶部 header 区增加一个细长的 **冷却能量条**
- 边框发光在冷却期间变为暗色，冷却完毕时闪烁恢复

#### c) 曲线颜色变化
- 冷却期间，canvas 上的当前曲线变为 **暗绿色** 或虚线
- 冷却完毕瞬间，曲线从暗绿脉冲闪亮为亮绿

---

## 七、音效增强

### 7.1 新增音效需求

| 音效       | 触发条件                 | 描述                     |
|-----------|-------------------------|-------------------------|
| 滑块拖动声  | 拖动参数滑块时            | 微弱的电子滴答声（类似雷达） |
| 锁定声     | 连击≥3                  | 短促的"叮"声             |
| 充能完成声  | 冷却结束                 | 上升音阶的电子音          |
| 低血量警报  | HP=1                    | 周期性低频蜂鸣            |
| 完美命中声  | 一发消灭≥5只怪物          | 爆炸低音 + 高频碎玻璃声   |

---

## 八、实现优先级建议

| 优先级 | 优化项                     | 影响范围          | 实现难度 |
|--------|---------------------------|------------------|---------|
| **P0** | 参数滑块双模输入            | 核心操作体验       | ⭐⭐      |
| **P0** | 发射后不清空参数            | 操作流畅度         | ⭐        |
| **P1** | 快速预设按钮               | 操作便利度         | ⭐        |
| **P1** | 命中粒子强化 + 冲击波       | 战斗爽感           | ⭐⭐      |
| **P1** | 浮动词数字弹出效果          | 战斗爽感           | ⭐⭐      |
| **P1** | 发射按钮冷却进度条          | 可视化             | ⭐        |
| **P2** | 曲线控制点拖拽             | 直观操作           | ⭐⭐⭐     |
| **P2** | 连击音效分层               | 战斗爽感           | ⭐⭐      |
| **P2** | Canvas 曲线控制点拖拽      | 直观操作           | ⭐⭐⭐⭐    |
| **P3** | 移动端触屏适配             | 平台扩展           | ⭐⭐⭐     |
| **P3** | 怪物预警线                 | 辅助判断           | ⭐⭐      |

---

## 九、技术实现要点

### 9.1 滑块组件封装

建议抽取 `ParamSlider` 通用组件：

```tsx
interface ParamSliderProps {
  label: string;
  value: number;
  min: number;
  max: number;
  step: number;
  shortcut: string;
  defaultValue: number;
  onChange: (value: number) => void;
}
```

- 内部使用 `<input type="range">` + `<input type="number">` 双绑
- 使用 `useRef` + `useEffect` 处理双向同步，避免无限循环
- 滑块 change 事件节流到 16ms（60fps）以避免频繁重绘曲线

### 9.2 浮动词伤害数字

使用独立的 overlay div 层（非 canvas），DOM 渲染浮动文字：

```tsx
interface FloatingText {
  id: string;
  x: number;      // canvas 坐标转屏幕坐标
  y: number;
  text: string;
  color: string;
  opacity: number;
  scale: number;
  createdAt: number;
}
```

- 存于组件 state 数组
- 使用 `requestAnimationFrame` 更新 opacity 和 y 偏移
- 1.5s 后自动移除

### 9.3 曲线控制点拖拽

- 在 canvas 上监听 `mousedown` / `touchstart`
- 判断点击位置是否在控制点半径内
- `mousemove` 时根据鼠标位置反算参数值
- `mouseup` 时结束拖拽

曲线坐标与参数的反算公式（以一次函数为例）：
- 控制点位于 y 轴交点 `(0, b)`
- 拖拽时直接根据鼠标 y 坐标反推 b 值
- `b_new = (height/2 - mouseY) / scale`

### 9.4 性能注意事项

- 滑块更新频次高，曲线重绘需要节流
- 粒子系统已有，扩展时注意粒子数量上限（建议 ≤300）
- 浮动词 DOM 数量控制（最多同时显示 10 个，旧的直接移除）
- 使用 `will-change: transform` 优化浮动词动画

---

## 十、总结

本优化方案围绕 **降低硬核门槛**、**增强战斗爽感**、**提升操作便利度** 三大目标，提出了从滑块输入、预设系统、视觉特效、音效反馈到触屏适配的全方位改进。

核心改变：
1. **参数输入从纯键盘变为滑块+数字双模**，大幅降低上手难度
2. **发射后不再清空参数**，让连续调整变得流畅
3. **增加丰富的命中/连击视觉特效**，让每次击杀都有爽快反馈
4. **预设系统和快捷曲线**让新手也能快速使用有效函数

建议按 P0 → P1 → P2 → P3 的顺序分阶段实施。