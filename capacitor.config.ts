import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.functiondefender.app',
  appName: 'Function Defender',
  webDir: 'dist'
};

export default config;
