@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;700;900&display=swap');
@import "tailwindcss";

@theme {
  --font-sans: "Noto Sans SC", ui-sans-serif, system-ui, sans-serif;
}


html, body {
  touch-action: manipulation;
  overscroll-behavior: none;
  -webkit-tap-highlight-color: transparent;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-5px) rotate(-1deg); }
  50% { transform: translateX(5px) rotate(1deg); }
  75% { transform: translateX(-5px) rotate(-1deg); }
}

.animate-shake {
  animation: shake 0.5s ease-in-out;
}

@keyframes glitch {
  0% { transform: translate(0) }
  20% { transform: translate(-2px, 2px) }
  40% { transform: translate(-2px, -2px) }
  60% { transform: translate(2px, 2px) }
  80% { transform: translate(2px, -2px) }
  100% { transform: translate(0) }
}

.animate-glitch {
  animation: glitch 0.3s infinite;
}

/* ===== P1 æµ®å¨æ°å­ / ç»ç«¯ä¼¤å®³æ°å­ ===== */
@keyframes fx-appear {
  0% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.25);
    filter: blur(6px);
  }
  40% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.25);
    filter: blur(0px);
  }
  65% {
    transform: translate(-50%, -50%) scale(0.95);
  }
  100% {
    transform: translate(-50%, -50%) scale(1);
  }
}

@keyframes fx-rise {
  0% {
    transform: translate(-50%, -50%) translateY(0) translateX(0);
  }
  20% {
    transform: translate(-50%, -50%) translateY(-12px) translateX(4px);
  }
  40% {
    transform: translate(-50%, -50%) translateY(-22px) translateX(-3px);
  }
  60% {
    transform: translate(-50%, -50%) translateY(-34px) translateX(2px);
  }
  80% {
    transform: translate(-50%, -50%) translateY(-46px) translateX(-1px);
  }
  100% {
    transform: translate(-50%, -50%) translateY(-58px) translateX(0);
  }
}

@keyframes fx-dissolve {
  0% {
    opacity: 1;
    filter: blur(0px) brightness(1.1);
  }
  60% {
    opacity: 0.85;
    filter: blur(1px) brightness(1.3);
  }
  100% {
    opacity: 0;
    filter: blur(5px) brightness(2);
  }
}

@keyframes fx-rgb-split {
  0% {
    text-shadow:
      -4px 0 0 rgba(255, 0, 60, 0.9),
      4px 0 0 rgba(0, 255, 255, 0.9),
      0 0 12px currentColor,
      0 0 28px currentColor,
      0 0 56px currentColor,
      0 0 90px currentColor;
  }
  35% {
    text-shadow:
      -1.5px 0 0 rgba(255, 0, 60, 0.6),
      1.5px 0 0 rgba(0, 255, 255, 0.6),
      0 0 10px currentColor,
      0 0 24px currentColor,
      0 0 48px currentColor,
      0 0 80px currentColor;
  }
  100% {
    text-shadow:
      0 0 0 transparent,
      0 0 0 transparent,
      0 0 16px currentColor,
      0 0 36px currentColor,
      0 0 72px currentColor,
      0 0 120px currentColor;
  }
}

@keyframes fx-scanline {
  0% {
    opacity: 0;
    transform: translateY(0);
  }
  10% {
    opacity: 1;
  }
  90% {
    opacity: 1;
  }
  100% {
    opacity: 0;
    transform: translateY(-55px);
  }
}

@keyframes fx-particle-burst {
  0% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1) rotate(0deg);
    filter: blur(0px);
  }
  25% {
    opacity: 0.98;
    transform: translate(calc(-50% + var(--tx, 20px) * 0.25), calc(-50% + var(--ty, 20px) * 0.25)) scale(1.35) rotate(var(--rotate-mid, 45deg));
    filter: blur(0.5px);
  }
  55% {
    opacity: 0.85;
    transform: translate(calc(-50% + var(--tx, 20px) * 0.7), calc(-50% + var(--ty, 20px) * 0.7)) scale(0.8) rotate(var(--rotate-mid, 45deg));
    filter: blur(2px);
  }
  100% {
    opacity: 0;
    transform: translate(calc(-50% + var(--tx, 20px)), calc(-50% + var(--ty, 20px))) scale(0.15) rotate(var(--rotate-end, 90deg));
    filter: blur(5px);
  }
}

.fx-floating-text {
  position: absolute;
  pointer-events: none;
  z-index: 25;
  font-family: 'Noto Sans SC', ui-monospace, SFMono-Regular, Menlo, monospace;
  font-weight: 900;
  white-space: nowrap;
  will-change: transform, opacity, filter;
  animation:
    fx-appear 0.18s cubic-bezier(0.18, 0.89, 0.32, 1.28) forwards,
    fx-rise 1.25s cubic-bezier(0.22, 0.61, 0.36, 1) 0.12s forwards,
    fx-dissolve 1.6s ease-in 0.35s forwards;
}

.fx-floating-text .fx-inner {
  display: inline-block;
  animation: fx-rgb-split 0.22s ease-out forwards;
}

.fx-floating-text::after {
  content: '';
  position: absolute;
  left: -10%;
  right: -10%;
  bottom: 0;
  height: 2px;
  background: currentColor;
  box-shadow: 0 0 10px currentColor, 0 0 20px currentColor;
  opacity: 0;
  animation: fx-scanline 1.1s ease-out 0.08s forwards;
}

.fx-particle {
  position: absolute;
  pointer-events: none;
  border-radius: 0;
  will-change: transform, opacity, filter;
  animation: fx-particle-burst var(--duration, 0.55s) ease-out forwards;
}

/* ===== P1 åå°æé® / è½éæ¡ ===== */
@keyframes bar-breathe {
  0%, 100% { box-shadow: 0 0 6px #00FF41, inset 0 0 8px rgba(0, 255, 65, 0.25); }
  50% { box-shadow: 0 0 18px #00FF41, inset 0 0 16px rgba(0, 255, 65, 0.45); }
}

@keyframes bar-shimmer {
  0% { transform: translateX(-120%); }
  100% { transform: translateX(120%); }
}

@keyframes bar-charge-flash {
  0% { opacity: 0; }
  15% { opacity: 1; }
  40% { opacity: 0.6; }
  60% { opacity: 1; }
  100% { opacity: 0; }
}

@keyframes border-sweep {
  0% { transform: rotate(0deg); opacity: 1; }
  100% { transform: rotate(360deg); opacity: 0; }
}

@keyframes fire-btn-idle {
  0%, 100% { box-shadow: 0 0 0 0 rgba(255, 176, 0, 0.0), inset 0 0 12px rgba(255, 176, 0, 0.15); }
  50% { box-shadow: 0 0 18px 2px rgba(255, 176, 0, 0.35), inset 0 0 18px rgba(255, 176, 0, 0.3); }
}

@keyframes spark-fall {
  0% {
    opacity: 1;
    transform: translate(0, 0) scale(1);
    filter: blur(0px);
  }
  40% {
    transform: translate(var(--tx-mid, 8px), -22px) scale(1.15);
  }
  100% {
    opacity: 0;
    transform: translate(var(--tx-end, 18px), 14px) scale(0.3);
    filter: blur(2px);
  }
}

.fx-energy-bar-segment {
  flex: 1;
  height: 100%;
  margin: 0 1.5px;
  border-radius: 1px;
  background: #0a1a0a;
  border: 1px solid rgba(0, 255, 65, 0.12);
  box-shadow: inset 0 0 4px rgba(0, 0, 0, 0.8);
  transition: background-color 0.08s ease, border-color 0.08s ease, box-shadow 0.08s ease;
}

.fx-energy-bar-segment.active {
  background: linear-gradient(180deg, #FFD700 0%, #FFB000 55%, #CC7700 100%);
  border-color: #FFD700;
  box-shadow: 0 0 8px #FFB000, 0 0 16px rgba(255, 176, 0, 0.55), inset 0 0 4px rgba(255, 255, 200, 0.6);
}

.fx-energy-bar-segment.active.pulse-tip {
  background: linear-gradient(180deg, #FFF 0%, #FFD700 40%, #FFB000 100%);
  box-shadow: 0 0 14px #FFD700, 0 0 28px rgba(255, 176, 0, 0.8), inset 0 0 6px #FFF;
}

.fx-fire-spark {
  position: absolute;
  width: 5px;
  height: 5px;
  border-radius: 50%;
  pointer-events: none;
  will-change: transform, opacity, filter;
  animation: spark-fall var(--duration, 0.7s) ease-out forwards;
}

.fx-corner-bracket {
  position: absolute;
  width: 10px;
  height: 10px;
  border-color: currentColor;
  pointer-events: none;
  transition: border-color 0.2s ease;
}

.fx-corner-bracket.tl { top: -2px; left: -2px; border-top: 2px solid; border-left: 2px solid; }
.fx-corner-bracket.tr { top: -2px; right: -2px; border-top: 2px solid; border-right: 2px solid; }
.fx-corner-bracket.bl { bottom: -2px; left: -2px; border-bottom: 2px solid; border-left: 2px solid; }
.fx-corner-bracket.br { bottom: -2px; right: -2px; border-bottom: 2px solid; border-right: 2px solid; }

/* ===== å¯æ»å¨å®¹å¨è§¦æ§è§å ===== */
aside,
.overflow-y-auto,
[class*="overflow-y-auto"] {
  touch-action: pan-y;
  overscroll-behavior: contain;
}

/* P0-1: æ»åææèªå®ä¹æ ·å¼ - åå½¢ï¼ç½è²è¾¹æ¡ + #00FF41 å¡«å */
input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #00FF41;
  border: 2px solid #FFFFFF;
  cursor: pointer;
  box-shadow: 0 0 8px #00FF41, 0 0 16px rgba(0, 255, 65, 0.5);
  transition: transform 0.15s ease;
}

input[type="range"]::-webkit-slider-thumb:hover {
  transform: scale(1.2);
  box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.7);
}

input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #00FF41;
  border: 2px solid #FFFFFF;
  cursor: pointer;
  box-shadow: 0 0 8px #00FF41, 0 0 16px rgba(0, 255, 65, 0.5);
}

input[type="range"]::-moz-range-thumb:hover {
  transform: scale(1.2);
  box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.7);
}

/* æ»åè½¨é */
input[type="range"]::-webkit-slider-runnable-track {
  height: 8px;
  border-radius: 4px;
}

input[type="range"]::-moz-range-track {
  height: 8px;
  border-radius: 4px;
}

/* ===== P0-2: åæ°é¢æ¿ä¸ç¨æ»åæ ·å¼ï¼ä¸å½±å header é³éæ»åï¼ ===== */
.param-slider {
  -webkit-appearance: none;
  appearance: none;
  height: 8px;
  border-radius: 4px;
  outline: none;
  cursor: pointer;
  border: 1px solid rgba(0, 255, 65, 0.25);
  box-shadow:
    0 0 6px rgba(0, 255, 65, 0.15),
    inset 0 0 4px rgba(0, 0, 0, 0.8);
  transition: box-shadow 0.2s ease;
}

.param-slider:focus,
.param-slider:hover {
  box-shadow:
    0 0 10px rgba(0, 255, 65, 0.35),
    inset 0 0 4px rgba(0, 0, 0, 0.8);
}

.param-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #00FF41;
  border: 2px solid #FFFFFF;
  cursor: pointer;
  box-shadow: 0 0 8px #00FF41, 0 0 16px rgba(0, 255, 65, 0.5);
  transition: transform 0.15s ease;
  margin-top: -5px; /* center 16px thumb on 8px track */
}

.param-slider::-webkit-slider-thumb:hover {
  transform: scale(1.2);
  box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.7);
}

.param-slider::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #00FF41;
  border: 2px solid #FFFFFF;
  cursor: pointer;
  box-shadow: 0 0 8px #00FF41, 0 0 16px rgba(0, 255, 65, 0.5);
  transition: transform 0.15s ease;
}

.param-slider::-moz-range-thumb:hover {
  transform: scale(1.2);
  box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.7);
}

.param-slider::-webkit-slider-runnable-track {
  height: 8px;
  border-radius: 4px;
  background: transparent;
}

.param-slider::-moz-range-track {
  height: 8px;
  border-radius: 4px;
  background: transparent;
}

/* ===== P3: ç§»å¨ç«¯è§¦å±éé ===== */
@media (pointer: coarse) {
  /* Canvas ç¦æ­¢æµè§å¨é»è®¤è§¦æ¸è¡ä¸ºï¼ç¼©æ¾/æ»å¨ï¼ï¼ç± JS èªè¡å¤çåæç¼©æ¾ */
  canvas {
    touch-action: none;
  }

  /* æ»åè½¨éå¢å¤§ï¼8px â 14px */
  input[type="range"]::-webkit-slider-runnable-track {
    height: 14px;
    border-radius: 7px;
  }

  input[type="range"]::-moz-range-track {
    height: 14px;
    border-radius: 7px;
  }

  /* æ»å thumb å¢å¤§ï¼16px â 32pxï¼æ»¡è¶³ â¥44px è§¦æ¸æ å */
  input[type="range"]::-webkit-slider-thumb {
    width: 32px;
    height: 32px;
    border: 3px solid #FFFFFF;
    box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.6);
  }

  input[type="range"]::-moz-range-thumb {
    width: 32px;
    height: 32px;
    border: 3px solid #FFFFFF;
    box-shadow: 0 0 12px #00FF41, 0 0 24px rgba(0, 255, 65, 0.6);
  }

  /* æ»åå®¹å¨å¢å  padding ä½¿æ»è§¦æ¸åºå â¥ 44px */
  input[type="range"] {
    padding-top: 6px;
    padding-bottom: 6px;
  }

  /* å½æ°éæ©æé®ï¼min-height: 44px + padding å¢å¤§ */
  aside button[id] {
    min-height: 44px;
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;
  }

  /* åå°æé®ï¼min-height: 56px */
  aside > div > div.space-y-3 > div.relative > button {
    min-height: 56px;
  }

  /* é¢è®¾æé® min-height 36px */
  aside button.flex-1 {
    min-height: 36px;
    padding-top: 0.375rem;
    padding-bottom: 0.375rem;
  }
}
